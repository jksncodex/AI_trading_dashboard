# TODO — Deploy to AWS (later phase)

Goal: run the dashboard non-stop on an AWS server.

Order of work when we get here:
1. Finish the app and tests
2. Docker (package the app)
3. GitHub Actions (auto-run tests)
4. Swap config url to PostgreSQL
5. AWS server, running 24/7